
var myArray = [5,10,4,2,60,100]; 

function sortList( a,b ){

    var arrayResult = [];

    for( var index = 0; index < myArray.length; index++ )
    {
        const element = array[index]; // 5
        for (let j = 0; j < array.length; j++) {

            if ( index != j )
            {
                var tmp = element - array[j];
            
                if( tmp > 0  )
                {

                }
            }
            
        }
    }
    return 
}